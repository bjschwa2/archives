int main(void)
{
	int fibonacci = (1 + Math.sqrt(5))/2;
	int invFib = 1/fibonacci;

	for (int i = 0; i< L ; i++){
		int total += key[i];
	}

	int hash = Math.floor(M * (total * invFib - Math.floor(total * invFib)));

	return hash;
}